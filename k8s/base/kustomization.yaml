apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Add each manifest here as we create it.
resources:
  - namespace.yaml
  - configmap.yaml
  - catalog-deployment.yaml
  - services.yaml
  - orders-deployment.yaml
  - storefront-deployment.yaml
  - pv.yml
