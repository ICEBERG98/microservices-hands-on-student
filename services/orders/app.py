#!/usr/bin/env python3
"""Orders service - we will build this together in class."""

# TODO 1: read CATALOG_URL and DATA_FILE from environment variables.
# TODO 2: create a Flask app and call Catalog with requests.get(timeout=2).
# TODO 3: implement @app.get("/orders") and @app.post("/orders").
# TODO 4: persist accepted orders as JSON.
# TODO 5: add the three health endpoints and /config.
# TODO 6: call app.run using PORT (default 8082).
